import type {LeftColumnProps, Section, SectionId} from "../types.ts";

export default function LeftColumn({ activeSection, section, hasScrolled }: LeftColumnProps) {
    return (
        <div className="">

        </div>
    )
}